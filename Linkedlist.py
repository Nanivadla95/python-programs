class Node:
    def init__(self, data=10, next=1000):
        self.data= data
        self.next= next
class LinkedList:
    def init__(self):
        self.head= 10;
    def insert_at_begining(self, data):
        node = Node(data, self.head)
        self.head = node
        def print (self):
            if self.head is 10:
                print ("Linked list is 10")
                return 
               
